TODO
notion. 
OKOCSTJS. 이하 OKOCS

caution.
운송장 실제 출력 금지

택배

자동화 1번
1. 홈페이지 (ongkihanyak.ac.kr/RAD/rankup_index/main.html)에서 주문관리-택배 엑셀다운(조제 중, 익산점 옵션)-엑셀다운 클릭 (택배관리_무슨점-날짜.xls). (크롬에서 로그인된 창이 항상 열려있다고 보장됨)
2. 해당 xls파일을 xlsx로 바꾸기 (이름 바꾸기 말고 안전한 변환 방식 필요)
3. 택배관리 파일의 1행을 없앰
4. G열의 “즉납” 키워드를 없애고 “4400’을 입력
5. OKOSC프로그램에서 처방전검색(출력)-대기처방-처방전송일자(7일 전 ~ 오늘, 조제)로 검색하여택배목록을 누르면 나오는 엑셀파일에서(그냥 뜸, 이름은 통합 문서 n) “통합 문서 n”의 이름의 떠있는 엑셀 창 중, 가장 n의 값이 큰 창에서 정보를 가져옴.
6. 통합 문서 파일의 E ~ I열을 택배관리 파일의 A ~ E열이 끝나는 행부터 연속적으로 붙여넣음
7. 통합 문서의 A, C ~ D가 택배관리 파일의 K, L ~ M에 연속적으로 정보를 옮김
8. F ~ H열의 내용이 아래쪽으로 붙여넣은 행 만큼 채워짐(1, 4400, 한약)
9. 택배관리 파일의 E열과 D열에 모두 번호가 있고 번호가 다르다면, 둘 다 유지. 두 열 중 하나만 번호가 있다면 모두 D 열로 이동 후 E열을 공란으로 둠
10. 로컬환경 Element > 한약국 > 옹기한약서류 > 익산대장$$ 있는 파일에서(이하 익산대장. 해당 경로에 익산대장~인 파일이 하나만 있다고 보장됨)
11. L열에 연한 녹색 바탕을 가진 텍스트(이름, 전화번호, 주소의 형식이 보장됨)를 택배관리 파일에 이름은 A열, 전화번호는 D열, 주소는 C열에 정보가 없는 가장 아래쪽 행에 붙여넣음(형식 상관 없음) FGH열은 그대로 따라옴(1, 4400, 한약)
12. 사람이 검토 (오케이 하면 아래 13, 14번 실행)
13. 로젠택베(ldgis.ilogen.com/commom/html/main.html)사이트로 감 (크롬에 로그인된 창이 항상 열려있다고 보장됨)
14.  예약관리-주문등록/출력(복수건)으로 감
15. 파일열기에서 택배관리 파일을 업로드
16. 사람이 검토 (오류 열에 오류가 없는지)

자동화 2번
1. 로젠택베(ldgis.ilogen.com)사이트에 예약관리-주문등록/출력(복수건)-엑셀저장 요청을 함
2. 다운로드에서 주문등록_출력(복수건)_출력완료$형식을 가진 가장 최근 파일을 대상으로 진행
3. 주문등록 파일을 csv 파일로 변환해서 저장 (데이터 손실이 없는 방법 필요)
4. 홈페이지 (ongkihanyak.ac.kr/RAD/rankup_index/main.html)에서 주문관리-로젠택배운송장업로드-파일선택에 파일 업로드
5. 확인버튼이 나타나면 바로 누르기
6. 주문등록_출력(복수건) 파일에서 s행의 주문번호가 옹기한약 사이트에 있는 고유번호와 같은데, 확인버튼을 누른 후 위쪽의 주문 상태가 있는 열의 정보를 택배 발송으로 바꾸기. 웹 구조에 고유번호가 있는 element가 고유번호 열의 element와 sibling 관계에 있음. 

자동화 3번
1. 주문등록_출력(복수건)_출력완료$에서 G 열에 있는 이름을 식별자로 하여 D열에서 얻은 운송장번호(연속된 숫자열)와 문자열을 결합하여 “로젠 000000”형식의 문자열을 얻음
2. OKOSC에서 처방전검색(출력)-대기처방-처방전송일자(7일 전 ~ 오늘, 조제)로 검색하여 나오는 창으로 감
3. 해당 행의 메모 수정-배송메모에 “로젠 000000”입력 후 저장

TODO : auto1의 okocs에서 조제 중인 정보를 가져올 때, 새로운 db 파일을 만들어서 주문 행의 첫 번째 열인 처방번호와 해당 프로그램에서 부여하는 네 자리 수(이하 autocode)를 매칭시켜 택배관리 파일의 j열에 okocs에서 가져온 요소에 대응되도록 autocode를 붙여넣음
이후, auto3에서 주문등록_출력(복수건)파일의 s열에 있는 4자리 오토코드를 db에서 상보적으로 연결된 처방번호에 해당하는 행에만 자동화 3번의 3번 동작을 실행함

보험한약


notion. 모든 수정과정에서는 시트 보호를 해제하고 편집이 끝나면 시트 보호를 다시 해야 함


1. OKOCS-처방전송일자-대기처방에 보험 행에 “보험”이라고 써져 있는 행에 대해 
2. 첩약보험약값대장.xlsx에서 가격표에 OKOCS-처방전검색(출력)-처방전송일자-대기처방(취소제외) 중 2열의 보험 열에 “보험”키워드가 있는 행 (이하 보험행)을 클릭하여 나오는 오른쪽에 있는 약재명/용량을 가격표 시트에 B열, C열 12번 행부터 시작하는 약재명, 1첩양 열에 쭉 나열 (괄호 안의 내용은 넣지 않음)
3. 만약 가격표 - B, C열에 내용을 다 넣고 어느 행에 E~L까지 공란이 있으면 사용자가 원산지를 확인하여야 함
4. 보험행 클릭 이후 나오는 아래쪽에서 환자명, 연락처, 주소를 파싱. 이름이 O6, 연락처가 O7, 주소가 O5으로 들어감
5. 출력-첩약보험(처방전)-인쇄를 클릭 (이하 처방전)
6. 처방전에서 주민번호, 한의원번호, 한의원 연락처, 한의사 이름, 한의사 면허번호, 한의원 기관번호를 추출하여 주민번호를 O8, 한의원명이 M6, 한의원 연락처가 M7, 한의사 이름을 M8, 한의사 면허번호를 M9, 한의원 기관번호 M10, 발급연월일(양식 : xxxx-xx-xx)을 O10, 발급번호(양식 : -xxxxx)를 P10에 넣음.
7. 처방전에서 기준처방명(string + alpha + int)인데, 그 중 string과 일치하는 것을 찾아서 선택
8. 처방전에서 질병 분류기호에 맞는 코드를 F5에서 선택하여 지정
9. 사용자가 기타 사항을 입력하고 확인함
10. 처방전에서 조제시 참고사항-1일복용팩수(양식: 1일복용팩수 (n 팩)를 T9, 용법의 내용을 T10에 넣음
11. OKOSC의 복용법의 내용을 가격표 시트의 T11에 들어감
12. 처방전-한약재 세부사항-한양재명칭및코드 열에서 요양급여비용명세서_양식 시트에 U16부터 시작하여 U 열에 한약재 이름, 처방전-1회 투약량 열을 Y16부터 Y열에 써넣음, 1일 투여횟수를 Z16부터 Z열에 써넣음
13. 처방전-총 투약횟수 열을 AA16부터 AA열에 써넣음
14. 처방전-가감 열을 AF16부터 AF 열에 써넣음 만약 AF 열의 내용 중 “감”키워드가 있다면 요양금여비용명세서_양식 시트의 1회 투약량(Y열)의 숫자가 음수가 되어야 함
15. R16셀 부터 R 열은 가격표 시트의 U16부터의 U 열에 한약재 이름에 해당하는 제품코드(W열)를 적음
16. 요양…의 단가(W열) 가격표 시트의 U16부터의 U 열에 한약재 이름에 해당하는 약재값(S16~열)의 숫자를 가져옴
17. 사람이 확인
18. 요양…시트의 A ~ AF열까지 복사하여 요양급여명세서모음2시트의 가장 오른쪽 내용이 없는 열에 붙여넣고 해당 내용을 인쇄 (테스트에서는 출력이라는 신호만 띄움)
19. 조제내역안내서_양식 시트의 A~W열을 복사하여 조제내역안내서양식_모음 시트의 내용이 없는 가장 오른쪽 열에 붙여넣음
20. 약재비영수증샘플(실비청구용)의 A ~ E열을 복사하여 약재비_영수증_모음의 내용이 없는 가장 오른쪽 행에 붙여넣음